"""Tools for studying a game before targeting it.

- ``script``: run a game under an emulator from a script of commands, with
  screenshots, savestates, memory reads and writes, and (mgba backend)
  breakpoints and watchpoints.
- ``mgba`` and ``libretro``: the emulator backends.
- ``scan``: scan an image for free space, pointers, pointer tables and text.
- ``tables``: text tables (``.tbl``) for decoding a game's own character codes.
"""
